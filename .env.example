# Copy this file to .env and adjust values

# Required
SESSION_SECRET=37AYGOhAyF62V^

# When running behind Synology Reverse Proxy (HTTPS)
TRUST_PROXY=1
COOKIE_SECURE=1

# Optional: override ports/paths if you change server defaults
# PORT=3000

# Optional: login rate limiting
# LOGIN_WINDOW_MS=900000
# LOGIN_MAX_ATTEMPTS=10

# Optional: directory for saving images with updated metadata.
# Provide an absolute path. Examples:
#   Windows: OUTPUT_DIR=C:/Users/rostg/SynologyDrive/Images/meta-set
#   Synology: OUTPUT_DIR=/volume1/homes/Images/meta-set
# If not set, the server picks a platform-specific default.
# OUTPUT_DIR=
